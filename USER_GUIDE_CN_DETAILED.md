# 短信充值卡助手操作指南（中文详细版）

## 一、功能简介

短信充值卡助手用于自动识别手机短信和 APP 通知中的交易信息。解析规则为后台下发的“交易模式”：短信消息依次尝试所有短信模式，APP 通知依次尝试所有 APP 通知模式，每个模式按“信息来源判断 → 交易信息判定 → 交易号/充值卡号/金额提取”执行，直至某个模式命中；交易号与交易金额由模式正则从消息正文提取，充值卡号在去除空格后的交易号上提取，并随消息记录提交到后台接口。

APP 支持以下能力：

- 首次使用设备号校验。
- 自动接收并识别短信。
- 在授权后监听 APP 通知。
- 解析交易号、充值卡号和交易金额。
- 标记新消息、重复消息和重复交易号。
- 查看本地消息记录和接口返回结果。
- 按日期筛选消息记录。
- 按天数批量清除历史消息。
- 重新绑定设备号。

说明：APP 名称固定使用英文名（SMS Recharge Card Assistant），界面支持多语言（中文、英语、法语、西班牙语、葡萄牙语）；APP 默认显示英文界面，首次打开的设备绑定页与主界面菜单“Language”均可选择语言，也可选择跟随系统语言；本指南使用中文说明对应功能。

## 二、首次使用与设备号验证

1. 安装并打开 APP。
2. 如果本机尚未保存有效设备号，APP 会自动进入设备号验证页面。
3. 在输入框中填写设备号。
4. 点击“Verify and Save”。
5. APP 会调用后台设备号校验接口 `checkDeviceIdValid.do`。
6. 如果接口返回成功，APP 会保存设备号、卖家ID以及后台下发的交易模式（解析规则），并进入主界面。
7. 如果接口返回失败，页面会显示错误信息，并停留在当前输入页面。

注意：

- 设备号未验证成功前，APP 不会启动短信拦截功能。
- 设备号未验证成功前，APP 不会启动 APP 通知拦截功能。
- 设备号验证失败时，请确认设备号是否正确，或联系后台管理员检查设备号是否已开通。
- 交易模式规则随设备号校验结果下发；后台调整规则后，重新绑定设备号即可同步。

## 三、开启短信识别权限

1. 进入主界面后，如果顶部显示权限提示，请点击“Grant Now”。
2. 系统会弹出权限申请窗口。
3. 请允许以下权限：
   - 接收短信。
   - 读取短信。
4. 授权完成后，APP 可以自动接收并识别符合规则的短信。

注意：

- 如果拒绝短信权限，APP 无法自动读取短信内容。
- 部分手机系统可能需要在系统设置中手动检查权限是否已开启。

## 四、开启 APP 通知监听权限

如果需要识别微信、支付宝或其他 APP 的通知，请开启通知监听权限。

操作步骤：

1. 进入 APP 的“Settings”页面。
2. 点击“Open Notification Listener Permission”。
3. 系统会跳转到通知访问权限页面。
4. 找到本 APP，并开启通知访问权限。
5. 返回 APP 后，通知识别功能即可生效。

注意：

- 通知监听权限必须由用户在系统设置中手动开启。
- Android 系统不支持通过普通权限弹窗直接授予通知监听权限。
- 如果通知内容没有出现在消息列表中，请确认通知权限是否已开启，并确认通知内容是否符合解析规则。

## 五、查看解析规则（交易模式）

解析规则由后台配置，并在设备号验证成功后自动下发并保存在 APP 中，本页面为只读展示。

进入“Settings”页面后，可以查看：

### 1. Trade Modes 模式按钮

- “Trade Modes”区域会列出后台下发的所有交易模式名称按钮。
- 点击某个模式按钮后，下方的“Mode Details”区域显示该模式的完整规则信息。
- 默认展示第一个模式的规则详情。
- 如果尚未获取到交易模式，会提示“No trade modes yet, please rebind the device ID to fetch them”。

### 2. 模式详情字段说明

- Name：模式名称。
- Notify Type：通知类型，SMS（短信）或 APP Notification（APP 通知）。
- Trade Info Source：信息来源判断；多个片段用英文逗号分隔，来源包含任一片段即通过，留空不限制。
- Trade Judge Rule：交易信息判定正则；消息正文匹配到该正则才视为该模式命中，留空表示不判定。
- Trade No. Parse Rule 1 / 2：交易号提取正则；Rule 2 非空时先按 Rule 1 提取，再对提取结果按 Rule 2 二次提取，最终以 Rule 2 结果为准。
- Card No. Parse Rule 1 / 2：充值卡号提取正则，输入为去除空格后的交易号，其余规则同上；交易号未提取出时卡号不再解析。
- Amount Parse Rule 1 / 2：交易金额提取正则，规则同上；未配置规则或未提取到时金额为空，不影响交易号和充值卡号的解析与提交。
- 未配置的字段显示“(not set)”。

### 3. 消息匹配顺序（后台解析逻辑）

1. 短信消息：依次尝试所有 Notify Type 为 SMS 的交易模式。
2. APP 通知：依次尝试所有 Notify Type 为 APP Notification 的交易模式。
3. 每个模式先做信息来源判断，再做交易信息判定；交易信息判定通过即视为该模式命中，停止遍历。
4. 命中后按该模式的交易号正则提取交易号，再在去除空格后的交易号上按充值卡号正则提取卡号；提取失败时记录失败原因，不再尝试其他模式。

### 4. 修改规则的方式

规则为只读展示，APP 内不提供修改入口。如需调整解析规则，请联系后台管理员在后台修改，修改后在本 APP 中重新绑定设备号即可同步最新规则。

## 六、重新获取最新规则

1. 后台调整交易模式后，进入“Settings”页面。
2. 点击“Rebind Device ID”。
3. 输入设备号并点击“Verify and Save”。
4. 验证成功后，APP 会从后台同步最新的交易模式。

说明：

- 规则以 JSON 列表形式随设备号校验结果下发，包含 name、notify_type、trade_info_source、trade_judge_rule、trade_no_parse_rule1、trade_no_parse_rule2、card_no_parse_rule1、card_no_parse_rule2、mony_parse_rule1、mony_parse_rule2 十个字段。
- 如果后台返回中不包含交易模式列表，APP 会保留本地已保存的交易模式。

## 七、测试解析

为了避免真实消息识别失败，建议先使用“Test Parse”功能验证规则。

操作步骤：

1. 进入“Settings”页面。
2. 选择消息类型：SMS（短信）或 APP Notification（APP 通知）。
3. 在“Source”输入框填写短信发送号码或 APP 名称/包名（可留空）。
4. 粘贴一条真实短信或 APP 通知内容。
5. 点击“Test Parse”。
6. APP 会显示解析结果。

解析结果可能包含：

- 是否命中某个交易模式，以及命中的模式名称。
- 交易号、充值卡号和交易金额（金额未提取到时显示 not extracted）。
- 命中但未提取出交易号或充值卡号时的失败原因。
- 未命中时的原因（包含各模式的来源/判定跳过说明）。

如果测试解析失败，请优先检查：

1. 所选消息类型（SMS / APP Notification）是否与消息实际来源一致。
2. 测试内容中的来源是否包含该模式“Trade Info Source”配置的任一片段。
3. 测试内容是否能被该模式“Trade Judge Rule”正则匹配。
4. 交易号正则能否从消息内容中提取到结果，以及充值卡号正则能否从去除空格后的交易号中提取到结果。
5. 如规则有明显不符，请联系后台管理员调整交易模式配置。

## 八、消息识别与后台提交

当 APP 收到短信或 APP 通知后，会按以下流程处理：

1. 判断设备号是否已验证通过。
2. 按消息类型（短信/APP 通知）依次尝试对应的交易模式，直至某个模式命中。
3. 命中后按模式正则从消息正文提取交易号，再在去除空格后的交易号上提取充值卡号，并按模式正则提取交易金额（金额未提取到时为空，不影响流程）。
4. 检查最近 1 小时是否存在相同消息内容。
5. 检查最近 1 小时是否存在相同交易号。
6. 保存本地记录。
7. 调用后台充值卡接口。
8. 保存接口返回结果。

消息类型说明：

- `0`：新消息。
- `1`：重复消息，即最近 1 小时内存在相同消息内容。
- `2`：重复交易号，即最近 1 小时内存在相同交易号。

## 九、查看消息列表

主界面会显示本地保存的消息记录，默认按接收时间倒序排列。

列表中通常包含：

- 接收时间。
- 消息来源。
- 消息类型。
- 来源标识。
- 交易号。
- 充值卡号。
- 交易金额。
- 处理状态。

处理状态说明：

- Processing：处理中。
- Success：处理成功。
- Failed：处理失败。
- Saved, Not Processed：已保存但未完整处理。

## 十、按日期筛选记录

1. 在主界面点击“Start Date”选择开始日期。
2. 点击“End Date”选择结束日期。
3. 点击“Query”。
4. APP 会显示该日期范围内的消息记录。
5. 点击“View All”可恢复显示全部记录。

## 十一、查看记录详情

1. 在主界面点击任意一条消息记录。
2. 进入详情页面后，可以查看完整消息内容和接口返回信息。
3. 如果后台接口返回失败，可在详情页查看错误原因。

详情页通常包含：

- 消息原文。
- 消息来源。
- 消息类型。
- 交易号。
- 充值卡号。
- 交易金额。
- 接口返回内容。
- 处理状态。

## 十二、清除历史消息

主界面支持按天数清除本地历史消息。

操作步骤：

1. 点击主界面工具栏中的“Clear Messages”。
2. 在弹窗中输入天数。
3. 例如输入 `7`，表示清除 7 天以前的消息。
4. 点击“Clear”。
5. APP 会删除符合条件的本地记录。
6. 删除完成后，APP 会提示实际清除的记录数量。

注意：

- 只能输入大于 0 的数字。
- 输入为空或小于等于 0 时，不会执行清除。
- 清除的是本地记录，不会删除后台数据。
- 删除完成后，消息列表会自动刷新。

## 十三、重新绑定设备号

如果需要更换设备号，可以重新绑定。

操作步骤：

1. 进入“Settings”页面。
2. 查看当前设备号。
3. 点击“Rebind Device ID”。
4. 输入新的设备号。
5. 点击“Verify and Save”。
6. 验证成功后，新设备号会保存到本机。

## 十四、常见问题

### 1. 为什么 APP 一直停留在设备号页面？

说明设备号还没有验证成功。请确认设备号是否正确，或联系后台管理员检查设备号是否已开通。

### 2. 为什么没有短信记录？

请检查：

- 是否已授权短信权限。
- 短信发送号码是否满足某个短信（SMS）模式的“Trade Info Source”判断。
- 短信正文是否能被某个短信（SMS）模式的“Trade Judge Rule”正则匹配。
- 是否已成功从后台获取到交易模式（可在“Settings”页面查看）。
- 设备号是否已验证成功。

### 3. 为什么没有 APP 通知记录？

请检查：

- 是否已开启通知监听权限。
- APP 名称或包名是否满足某个 APP Notification 模式的“Trade Info Source”判断。
- 通知正文是否能被某个 APP Notification 模式的“Trade Judge Rule”正则匹配。
- 是否已成功从后台获取到交易模式（可在“Settings”页面查看）。
- 设备号是否已验证成功。

### 4. 为什么解析不到交易号？

请检查命中模式的“Trade No. Parse Rule”正则能否从消息内容中提取到交易号；如规则与消息格式不符，请联系后台管理员调整交易模式配置。

### 5. 为什么解析不到金额？

请检查命中模式的“Amount Parse Rule”正则能否从消息内容中提取到金额；金额未提取到时为空字符串，不影响交易号、充值卡号的解析与提交。如规则与消息格式不符，请联系后台管理员调整交易模式配置。

### 6. 为什么记录显示 Failed？

可能是后台接口返回失败或网络异常。请进入记录详情查看接口返回内容。

### 7. 清除消息后还能恢复吗？

不能。清除消息会删除本地数据库中的记录。请谨慎操作。
