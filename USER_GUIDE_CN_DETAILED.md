# 短信充值卡助手操作指南（中文详细版）

## 一、功能简介

短信充值卡助手用于自动识别手机短信和 APP 通知中的交易信息。APP 会根据配置的来源白名单、正文关键词、交易单号关键字和交易单号长度解析消息，解析成功后取交易单号最后 6 位作为充值卡号，并将消息记录提交到后台接口。

APP 支持以下能力：

- 首次使用设备号校验。
- 自动接收并识别短信。
- 在授权后监听 APP 通知。
- 解析交易单号、充值卡号和交易金额。
- 标记新消息、重复消息和重复交易号。
- 查看本地消息记录和接口返回结果。
- 按日期筛选消息记录。
- 按天数批量清除历史消息。
- 重新绑定设备号。

说明：APP 页面文案为英文，本指南使用中文说明对应功能。

## 二、首次使用与设备号验证

1. 安装并打开 APP。
2. 如果本机尚未保存有效设备号，APP 会自动进入设备号验证页面。
3. 在输入框中填写设备号。
4. 点击“Verify and Save”。
5. APP 会调用后台设备号校验接口 `checkDeviceId.do`。
6. 如果接口返回成功，APP 会保存设备号并进入主界面。
7. 如果接口返回失败，页面会显示错误信息，并停留在当前输入页面。

注意：

- 设备号未验证成功前，APP 不会启动短信拦截功能。
- 设备号未验证成功前，APP 不会启动 APP 通知拦截功能。
- 设备号验证失败时，请确认设备号是否正确，或联系后台管理员检查设备号是否已开通。

## 三、开启短信识别权限

1. 进入主界面后，如果顶部显示权限提示，请点击“Grant Now”。
2. 系统会弹出权限申请窗口。
3. 请允许以下权限：
   - 接收短信。
   - 读取短信。
4. 授权完成后，APP 可以自动接收并识别符合规则的短信。

注意：

- 如果拒绝短信权限，APP 无法自动读取短信内容。
- 部分手机系统可能需要在系统设置中手动检查权限是否已开启。

## 四、开启 APP 通知监听权限

如果需要识别微信、支付宝或其他 APP 的通知，请开启通知监听权限。

操作步骤：

1. 进入 APP 的“Settings”页面。
2. 点击“Open Notification Listener Permission”。
3. 系统会跳转到通知访问权限页面。
4. 找到本 APP，并开启通知访问权限。
5. 返回 APP 后，通知识别功能即可生效。

注意：

- 通知监听权限必须由用户在系统设置中手动开启。
- Android 系统不支持通过普通权限弹窗直接授予通知监听权限。
- 如果通知内容没有出现在消息列表中，请确认通知权限是否已开启，并确认通知内容是否符合解析规则。

## 五、配置解析规则

进入“Settings”页面后，可以配置以下规则。

### 1. Source whitelist

来源白名单，用于限制哪些短信号码、APP 名称或 APP 包名可以被识别。

填写规则：

- 多个来源用英文逗号分隔。
- 短信发送号码包含任意一个来源片段时通过。
- APP 名称或包名包含任意一个来源片段时通过。
- 留空表示不限制来源。

示例：

```text
WECHAT,微信,com.tencent.mm
```

### 2. Message content filter keywords

消息正文筛选关键词，用于判断消息内容是否需要处理。

填写规则：

- 多个关键词用英文逗号分隔。
- 所有关键词都必须出现在消息正文中，消息才会被处理。
- 匹配时不区分大小写。

### 3. Order No. Keyword

交易单号关键字。APP 会在消息中查找该关键字，并从关键字后方截取指定长度的内容作为完整交易单号。

示例：

```text
交易单号
```

### 4. Order No. Length

交易单号长度。APP 从交易单号关键字后方截取该长度的字符串作为完整交易单号。

示例：

```text
20
```

解析成功后，APP 会取完整交易单号的最后 6 位作为充值卡号。

### 5. Amount keyword

交易金额关键字。APP 会在该关键字后方提取数字作为交易金额。

说明：

- 支持整数。
- 支持带一个小数点的金额。
- 未解析到金额时，金额为空，不影响消息保存和接口提交。

### 6. Backend API URL

后台充值卡接口地址。APP 识别到消息后，会将消息内容、交易单号、充值卡号、金额、设备号和消息类型提交到该接口。

### 7. Device ID Verification API URL

设备号校验接口地址。首次使用或重新绑定设备号时，APP 会调用该接口验证设备号是否有效。

## 六、保存配置

1. 在“Settings”页面修改配置。
2. 点击“Save”。
3. APP 提示保存成功后，新配置生效。

如果交易单号关键字为空，或交易单号长度不是大于 0 的数字，APP 会提示错误，不会保存无效配置。

## 七、测试解析

为了避免真实消息识别失败，建议先使用“Test Parse”功能验证配置。

操作步骤：

1. 进入“Settings”页面。
2. 在测试输入框中粘贴一条真实短信或 APP 通知内容。
3. 点击“Test Parse”。
4. APP 会显示解析结果。

解析结果可能包含：

- 是否命中来源和关键词。
- 是否解析出交易单号。
- 完整交易单号。
- 充值卡号后 6 位。
- 交易金额。
- 后续处理动作。
- 解析失败原因。

如果测试解析失败，请优先检查：

1. 来源白名单是否包含短信号码、APP 名称或 APP 包名。
2. 正文关键词是否全部出现在消息内容中。
3. 交易单号关键字是否与消息内容一致。
4. 交易单号长度是否正确。
5. 交易金额关键字是否与金额前缀一致。

## 八、消息识别与后台提交

当 APP 收到短信或 APP 通知后，会按以下流程处理：

1. 判断设备号是否已验证通过。
2. 判断消息来源是否命中来源白名单。
3. 判断消息正文是否包含全部配置关键词。
4. 尝试解析交易单号。
5. 尝试解析交易金额。
6. 检查最近 1 小时是否存在相同消息内容。
7. 检查最近 1 小时是否存在相同交易单号。
8. 保存本地记录。
9. 调用后台充值卡接口。
10. 保存接口返回结果。

消息类型说明：

- `0`：新消息。
- `1`：重复消息，即最近 1 小时内存在相同消息内容。
- `2`：重复交易号，即最近 1 小时内存在相同交易单号。

## 九、查看消息列表

主界面会显示本地保存的消息记录，默认按接收时间倒序排列。

列表中通常包含：

- 接收时间。
- 消息来源。
- 消息类型。
- 来源标识。
- 交易单号。
- 充值卡号。
- 交易金额。
- 处理状态。

处理状态说明：

- Processing：处理中。
- Success：处理成功。
- Failed：处理失败。
- Saved, Not Processed：已保存但未完整处理。

## 十、按日期筛选记录

1. 在主界面点击“Start Date”选择开始日期。
2. 点击“End Date”选择结束日期。
3. 点击“Query”。
4. APP 会显示该日期范围内的消息记录。
5. 点击“View All”可恢复显示全部记录。

## 十一、查看记录详情

1. 在主界面点击任意一条消息记录。
2. 进入详情页面后，可以查看完整消息内容和接口返回信息。
3. 如果后台接口返回失败，可在详情页查看错误原因。

详情页通常包含：

- 消息原文。
- 消息来源。
- 消息类型。
- 交易单号。
- 充值卡号。
- 交易金额。
- 接口返回内容。
- 处理状态。

## 十二、清除历史消息

主界面支持按天数清除本地历史消息。

操作步骤：

1. 点击主界面工具栏中的“Clear Messages”。
2. 在弹窗中输入天数。
3. 例如输入 `7`，表示清除 7 天以前的消息。
4. 点击“Clear”。
5. APP 会删除符合条件的本地记录。
6. 删除完成后，APP 会提示实际清除的记录数量。

注意：

- 只能输入大于 0 的数字。
- 输入为空或小于等于 0 时，不会执行清除。
- 清除的是本地记录，不会删除后台数据。
- 删除完成后，消息列表会自动刷新。

## 十三、重新绑定设备号

如果需要更换设备号，可以重新绑定。

操作步骤：

1. 进入“Settings”页面。
2. 查看当前设备号。
3. 点击“Rebind Device ID”。
4. 输入新的设备号。
5. 点击“Verify and Save”。
6. 验证成功后，新设备号会保存到本机。

## 十四、常见问题

### 1. 为什么 APP 一直停留在设备号页面？

说明设备号还没有验证成功。请确认设备号是否正确，或联系后台管理员检查设备号是否已开通。

### 2. 为什么没有短信记录？

请检查：

- 是否已授权短信权限。
- 短信发送号码是否命中来源白名单。
- 短信正文是否包含全部关键词。
- 设备号是否已验证成功。

### 3. 为什么没有 APP 通知记录？

请检查：

- 是否已开启通知监听权限。
- APP 名称或包名是否命中来源白名单。
- 通知正文是否包含全部关键词。
- 设备号是否已验证成功。

### 4. 为什么解析不到交易单号？

请检查交易单号关键字和交易单号长度是否与真实消息内容一致。

### 5. 为什么解析不到金额？

请检查交易金额关键字是否与真实消息内容中的金额前缀一致。

### 6. 为什么记录显示 Failed？

可能是后台接口返回失败或网络异常。请进入记录详情查看接口返回内容。

### 7. 清除消息后还能恢复吗？

不能。清除消息会删除本地数据库中的记录。请谨慎操作。
